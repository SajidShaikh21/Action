name: Action-workflow

on: 
  push:

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: create file
        run:  echo "This is test report" > report.txt



  second-job:
    runs-on: ubuntu-22.04
    needs: first-job

    steps:
      - name: viewing report from first job
        run:  cat report.txt
