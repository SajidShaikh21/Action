name: b11-workflow

on: push

jobs:
  first-job: 
    runs-on: ubuntu-latest

    steps: 
       - name: create a file 
         run: echo "This Is Text reports" > report.txt


  second-job:
    runs-on: ubuntu-latest
    needs: first-job
    steps:
      - name: viewing report from first job
        run: cat report.txt

